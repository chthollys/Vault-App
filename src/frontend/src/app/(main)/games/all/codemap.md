# Codemap

## Purpose
All-games listing route (`/games/all`).

## Files
- `page.tsx`: Server route composition for title/filter controls and infinite grid body.

## Subdirectories
- `components`: Main listing grid + title/sort UI components.
