# Codemap

## Purpose
Feature components for the `/games/all` route.

## Files
- `MainGrid.tsx`: Client container that reads URL query params and renders suspense-wrapped infinite games grid.

## Subdirectories
- `Title`: Breadcrumb, title, and sort-select controls.
